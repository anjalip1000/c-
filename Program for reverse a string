#include<bits/stdc++.h>
using namespace std;
int main()
{
    string str ;
    int i;
    cin >> str;
   string str_rev;
    for(i=str.size()-1;i>=0;i--)
    {
        str_rev=str_rev+str[i];
    }

    std::cout << str_rev << std::endl;
    
    return 0;
    
    
}

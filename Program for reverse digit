#include <iostream>
#include<math.h>
#include <bits/stdc++.h>
using namespace std;
int main() {
       int ans=0;
	int n;
	cin>>n;
	while(n!=0){
	       int digit=n%10;
	       ans=(ans*10)+digit;
	       n=n/10;
	}
	std::cout << ans << std::endl;
	return 0;
}
